'use strict';

let generatedNumbers;
let attemptsCount;

resetGame();

function resetGame() {
    attemptsCount = 0;
    generatedNumbers = [];

    while (generatedNumbers.length < 4) {
        const part = Math.floor(Math.random()*10);

    }
}